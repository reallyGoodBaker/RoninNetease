# `architect.ref` 模块

资源与引用管理相关的工具集合，封装对共享资源的访问与引用计数逻辑。

源文件： `architect/ref.py`

核心类型：

- `Ref` — 简单的可变引用封装
	- 构造：`Ref(value)`
	- 属性：`value`（可读写）
	- `isValid()` — 判断内部值是否非空

用途：`Ref` 常用于在事件或链式调用中传递可变的控制标志（例如中断事件流的布尔引用）。
